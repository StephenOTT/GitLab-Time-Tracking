
<% if current_user != nil %>

	<h2>Download Time Tracking Data your GitLab Instance</h2>

	<form class="form-horizontal" role="form", action="/gl-download", method="post">

		<div class="form-group">
			 <label for="repo" class="col-sm-2 control-label">Avaliable Projects</label>
			<div class="col-sm-10">
				<select name="post[repo]", id="repo">
					<% @projectList.each do |p| %>
					<%= p = p.to_h %>
						<option value= "<%= "#{p['id']}" %>" ><%= p["path_with_namespace"] %></option>
					<% end %>
				</select>
			</div>
		</div>

<!-- 		<div class="form-group">
			 <label for="projectid" class="col-sm-2 control-label">Project ID</label>
			<div class="col-sm-10">
				<input type="text" class="form-control" id="projectid", name="post[projectid]" value="153287" />
				<p>For test purposes see project stephenott/steve (project id: 153287)</p>
			</div>
		</div> -->

		<div class="form-group">
			<div class="col-sm-offset-2 col-sm-10">
				 <button type="submit" class="btn btn-default">download</button>
			</div>
		</div>
	</form>
	<hr>
<% end %>